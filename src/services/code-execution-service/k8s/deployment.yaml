apiVersion: apps/v1
kind: Deployment
metadata:
  name: code-execution-service
  namespace: learnflow
spec:
  replicas: 2
  selector:
    matchLabels:
      app: code-execution-service
  template:
    metadata:
      labels:
        app: code-execution-service
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "code-execution-service"
        dapr.io/app-port: "8000"
        dapr.io/log-level: "info"
    spec:
      containers:
      - name: code-execution-service
        image: code-execution-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        env:
        - name: DAPR_HTTP_PORT
          value: "3500"
        - name: DAPR_GRPC_PORT
          value: "50001"
        - name: EXEC_TIMEOUT
          value: "10"
        - name: MAX_OUTPUT_SIZE
          value: "10000"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: postgres-credentials
              key: DATABASE_URL
              optional: true
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: code-execution-service
  namespace: learnflow
spec:
  selector:
    app: code-execution-service
  ports:
  - port: 80
    targetPort: 8000
  type: ClusterIP
