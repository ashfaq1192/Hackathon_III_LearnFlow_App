# LearnFlow Environment Configuration
# Copy to .env and fill in values: cp .env.example .env

# Required: OpenAI API key for AI-powered services (triage, concepts)
OPENAI_API_KEY=sk-your-openai-api-key

# Required: Neon PostgreSQL connection string
DATABASE_URL=postgresql://user:password@ep-xxxx.us-east-2.aws.neon.tech/learnflow?sslmode=require

# Optional: Better Auth session secret (auto-generated if not set)
AUTH_SECRET=

# Optional: OAuth providers for Better Auth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
